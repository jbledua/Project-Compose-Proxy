# Stack
STACK_NAME=proxy
PROXY_NETWORK=proxy
DYNAMIC_DIR=./dynamic

# Traefik
LE_EMAIL=you@example.com
HTTP_PORT=8080
HTTPS_PORT=8443

# Cloudflare (DNS-01 for Let's Encrypt)
# Create a scoped token with Zone:Read + DNS:Edit for ledua.ca
CF_DNS_API_TOKEN=cf_xxx

# Cloudflare Tunnel (Zero Trust > Access > Tunnels > Create; copy the token)
CF_TUNNEL_TOKEN=eyJhIjoi...long-token...

# Tailscale backup sidecar (optional, but matches your usual pattern)
TS_HOSTNAME=proxy-host
TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxxxxx
PUBLIC_PORT=9926
