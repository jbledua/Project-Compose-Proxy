STACK_NAME=proxy

# Dry-run ports (8080/8443). Cutover: set to 80/443 and redeploy.
HTTP_PORT=8080
HTTPS_PORT=8443

# Email for Let's Encrypt registration/renewals
LE_EMAIL=admin@example.com

# Absolute host path for your dynamic rules folder (must exist on the host)
DYNAMIC_DIR=/srv/traefik/dynamic
